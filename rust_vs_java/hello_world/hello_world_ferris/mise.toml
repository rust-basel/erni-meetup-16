[tools]
rust = { version = "1.97" }
hyperfine = { version = "1.20" }

[tasks.run]
run = 'cargo install --path . && hyperfine --runs 1 "ferris"'

[tasks.bench]
# compile then run the binary
run = 'cargo install --path . && hyperfine --runs 500 --warmup 100 "ferris"'
